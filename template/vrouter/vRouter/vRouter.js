const { defaultFormatUtc } = require("moment");

export default {
    props:{
        vr_handle: {type:Object,required:true,},
        vr_route: {type:Object,required:true,},
        vr_router: {type:Object,required:true,},
    },

    methods:{
        // back
        // go
        // push
        // replace
        // resolve
        // currentRoute
    }
}